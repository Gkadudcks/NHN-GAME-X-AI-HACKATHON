# DAY 4 퇴근 미니게임 구현 요구사항

이 문서는 `부장님 피해서 퇴근하기`의 기획·구현·검증 단일 기준이다. 대화 컨텍스트가 압축되거나 새 작업에서 루프를 재개할 때 먼저 읽는다.

## 1. 확정 방향

- 장르: 2D 횡스크롤 자동 달리기
- 플레이 시간: 기본 64초, 허용 범위 60~75초
- 입력: 점프 + 슬라이드. 좌우 이동 없음
- 구간: 사무실 → 회의실·복합기 복도 → 엘리베이터 로비
- 감정: “오늘만은 정시에 퇴근한다”는 가벼운 긴장과 오피스 코미디
- 레퍼런스 사용법:
  - Mario: 첫 위험은 쉽게, 위험의 시각 언어를 반복 학습, 실패 후 바로 회복
  - Mario Run: 자동 전진으로 입력을 한두 개의 의미 있는 타이밍에 집중
  - Celeste: 입력 버퍼·코요테 타임·관대한 충돌 보정
  - Danganronpa V3 Death Road of Despair: 동료가 함께 달리는 탈출 압박과 연속 함정의 리듬만 취하고, 의도적 초고난도·가려진 즉사는 제외
  - 제외: 의도적 불공정, 미끄러운 관성, 암기 전제 즉사, 긴 재시작

## 2. 플레이 규칙

### 이동과 입력

- 도윤은 자동으로 오른쪽으로 달린다.
- `Space`, `ArrowUp`, `W` 또는 점프 버튼으로 점프한다.
- `ArrowDown`, `S` 또는 슬라이드 버튼을 누르는 동안 슬라이드한다.
- 점프 입력 버퍼 150ms, 코요테 타임 120ms를 제공한다.
- 위험 오브젝트의 실제 충돌 상자는 보이는 크기보다 15~20% 작게 둔다.

### 화면 좌표와 입력 감각

- 도윤의 화면상 발바닥 중심 X는 플레이필드 너비의 30~35%에 고정하며, 정상 플레이 중 기준점 오차는 ±2px 이하여야 한다.
- 점프는 도윤의 Y축만 바꾸고, 자동 전진은 배경·바닥·장애물의 공통 월드 이동으로 표현한다. 피격 반동 외의 공중 좌우 이동과 air steering은 두지 않는다.
- 입력 이벤트부터 첫 시각 반응은 다음 렌더 프레임 이내, 실제 상승 시작은 50ms 이내여야 한다.
- 짧게 누른 점프와 길게 누른 점프의 최고점 차이는 720p 기준 55px 이상이어야 한다. 길게 누를 때 정점 부근 중력을 기본값의 0.5~0.65배로 낮춰 착지 조정 시간을 제공한다.
- 슬라이드 판정과 전용 포즈는 같은 프레임에 바뀌며, 시작과 해제 반응은 각각 80ms 이하여야 한다. 정지 원화를 회전·기울여 슬라이드로 대체하지 않는다.
- 점프·슬라이드 입력은 키보드와 터치에서 같은 상태 전이와 타이밍 규칙을 사용한다.

### 위험과 회복

- 낮은 장애물은 점프, 머리 높이 장애물은 슬라이드로 피한다.
- 새 위험 유형은 안전한 단독 예시로 먼저 소개한 뒤 조합한다.
- 새 위험은 충돌 900~1200ms 전, 이미 학습한 위험은 최소 700ms 전에 실루엣·행동 아이콘·바닥 표식 중 두 가지로 예고한다.
- 한 패턴은 준비 0.9~1.2초 → 실행 0.25~0.6초 → 회복 0.5초 이상의 리듬을 가진다. 첫 등장 위험 사이에는 1.2초, 서로 다른 행동을 잇는 첫 조합에는 1.5초 이상의 여유를 둔다.
- 모든 장애물은 공통 월드 속도에 묶고, 화면 오른쪽에서 충돌 지점까지 끊김 없이 이동한다. 타입별 독립 슬라이드 애니메이션이나 갑작스러운 완성 크기 출현은 금지한다.
- 피격 후 800ms 무적과 짧은 속도 저하를 제공하고 플레이는 계속된다.
- 서하린은 항상 동행하며 첫 위기 안내와 1회의 자동 보조를 제공한다.
- 보조 성능은 호감도와 무관하며, 호감도는 대사 표현에만 영향을 준다.

### 판정과 피격 가독성

- 플레이어 이미지와 논리 body는 같은 하단 중앙 앵커를 사용하며, 화면상 발바닥 중심 오차는 4px 이하여야 한다.
- 장애물 판정은 불투명 위험 실루엣 안에 있어야 하며, 보이는 기준 상자에서 양축 총 18% inset 규칙을 유지한다.
- dev 화면에는 플레이어 body, 장애물 기준 상자, 실제 충돌 상자를 구분해 표시하는 `Hitbox` 토글을 둔다. 본편에서는 디버그 상자를 노출하지 않는다.
- 본편에서는 바닥 그림자, 통과 여유선, 접촉 스파크를 실제 판정 위치에 맞춰 그림만 보고도 안전 범위를 예상할 수 있게 한다.
- 피격 시 40~60ms의 짧은 정지, 접촉점 효과, 캐릭터 플래시를 제공하되 조작 복귀와 800ms 무적 규칙은 유지한다.

### 수집과 결과

- 선택 수집물: 출입카드, 휴대폰, 백업 USB.
- 수집물은 결과 상세와 콤보에만 반영하고 등급·스토리 진행을 잠그지 않는다.
- 피격 0회: `perfect`
- 피격 1~2회: `close`
- 피격 3회 이상: `caught`
- `caught`도 정상 완료이며 “10분 확인 업무 후 퇴근”으로 본편에 복귀한다.
- 결과 계약:

```js
{
  grade: "perfect" | "close" | "caught",
  caught: boolean,
  elapsed: number,
  hitCount: number,
  collectedItems: string[],
  maxCombo: number
}
```

## 3. 화면과 아트

- 기존 승인 배경 안정 ID를 세 구간의 원경·색감 기준으로 재사용하되, 한 장의 고정 배경만으로 코스를 구성하지 않는다.
  - `background.office.evening`
  - `background.meeting_room.afternoon`
  - `background.elevator_lobby.night`
- 도윤은 측후면 중심으로 얼굴 노출을 줄이고 자연스러운 성인 비율을 유지한다.
- 캐릭터, 배경, 소품을 각각 별도 에셋으로 관리한다.
- `distance`에 연결된 최소 3층 패럴랙스를 사용한다. 권장 속도 계수는 원경 0.15, 중경 0.4, 바닥·장애물 1.0이다.
- 바닥 이음선과 가구 열은 매 프레임 이동해야 하며, 8~12초마다 문·유리 파티션·출입 게이트·엘리베이터 문턱 중 하나가 도윤을 실제로 지나가야 한다.
- 구간 전환은 배경 페이드만으로 처리하지 않는다. 사무실 → 복도 → 엘리베이터 로비의 공간·시간대가 순방향으로 이어져야 한다.
- 플레이 중 지속 HUD는 데스크톱 플레이필드의 16%, 모바일의 20% 이하로 제한한다. 위험·진행·수집만 상단 한 줄에 남기고 데스크톱 하단 터치 패널은 숨긴다.
- 행동 명령은 데스크톱 24px·모바일 20px 이상, 위험·진행 수치는 18px 이상, 필수 조작 터치 영역은 최소 48×48px로 한다.
- 위험 예고는 우측 고정 웹 카드가 아니라 해당 장애물의 바닥·머리 높이에 붙인다. 플레이필드 중앙 55%와 도윤 진행 방향은 상시 패널로 가리지 않는다.
- 캐릭터 시드 프레임이 승인되기 전에는 애니메이션 스트립을 생성하지 않는다.
- 런타임 캐릭터에는 VN 대화용 정면 초상화를 사용하지 않는다. 기존 계획 ID `minigame_character.doyun.jump.right`, `minigame_character.doyun.slide.right`, `minigame_character.harin.run.right`, `minigame_character.harin.assist.right`, `minigame_character.boss.chase.right`, `minigame_character.boss.call.right`를 채워 재사용한다.
- 도윤·하린·부장님은 동일한 우향 3/4 시점, 발바닥 기준선, 외곽선 굵기, 명도 대비를 사용한다. established 얼굴·머리·체형·복장·소품은 변경하지 않는다.
- 달리기·추격 상태는 방향을 뒤집지 않고 동일한 우향을 유지한 두 보폭 프레임을 160ms마다 교대한다. 점프·슬라이드·도움·호출 상태에서는 해당 전용 정지 포즈를 유지한다.
- 보조 보폭이 없거나 이미지 로드가 실패하거나 `prefers-reduced-motion`이 켜져 있으면 승인된 기본 프레임 하나로 안전하게 폴백한다.
- 승인 전 draft/review 파일은 본편 런타임의 `active_version`으로 지정하지 않는다.
- 이미지 생성·수정은 `docs/art/` 규칙과 `assets/art/manifests/art-assets.json`을 따른다.

## 4. 구조와 호환성

- 본편 공개 API `OfficeEscapeMinigame.start({ onComplete })`, `pause()`, `resume()`을 유지한다.
- 순수 규칙은 `office-escape-minigame-core.js`, DOM·입력·연출은 `office-escape-minigame.js`에 둔다.
- DAY 4 본편의 저장 필드 `minigameResult`와 기존 `grade`, `caught` 의미를 유지하며 결과 필드를 확장한다.
- 개발 화면은 `NAN_GAME_TITLE/dev/day4-office-escape-minigame.html`로 만든다.
- 공용 개발 도구 스타일은 `css/minigame-dev.css`로 분리해 DAY 3 개발 화면도 재사용한다.
- 테스트 전용 옵션은 공개 결과 계약을 바꾸지 않으며 dev 화면에서만 주입한다.

## 5. 루프 지침

1. 이 문서, `AGENTS.md`, 관련 기존 코드, `docs/art/`를 읽는다.
2. 변경하려는 책임을 가진 기존 모듈이 있는지 먼저 검색한다.
3. 한 루프에서 게임 감각 또는 시각 요소의 큰 축 하나만 바꾼다.
4. 규칙 변경은 core 테스트, 통합 변경은 DAY 4 테스트를 먼저 갱신한다.
5. 이미지 변경 시 manifest 계획 → 생성 로그 → draft/review → 검증 → 사용자 승인 → approved/active 순서를 지킨다.
6. `python scripts/validate_art_assets.py`와 `node --test tests/*.test.js`를 실행한다.
7. 데스크톱·모바일에서 플레이하고 시작, 입력, 피격, 구간 전환, 결과, 재시작, 일시정지를 확인한다.
8. 변경 이유, 관찰 결과, 남은 문제를 이 문서의 결정 로그에 짧게 기록한다.
9. 테스트·아트 검증·브라우저 확인 중 하나라도 실패하면 완료로 선언하지 않는다.

## 6. 완료 기준

- 첫 플레이어가 설명을 읽지 않아도 첫 두 장애물로 점프와 슬라이드를 학습한다.
- 정상 플레이가 60~75초 안에 끝난다.
- 세 구간의 장소·위험·속도 리듬이 구분된다.
- 피격 뒤 조작권이 즉시 돌아오고 연속 피격이 발생하지 않는다.
- 키보드와 터치에서 모든 필수 행동을 수행할 수 있다.
- 결과 객체와 DAY 4 저장·복귀 흐름이 테스트로 보장된다.
- 본편에 쓰이는 모든 이미지가 승인된 안정 ID로 해석된다.
- 전체 플레이에서 도윤 화면 X가 ±2px 이내이고 점프 시 Y만 변한다.
- 점프 입력 반응 50ms 이하, 짧은/긴 점프 최고점 차이 55px 이상, 슬라이드 시작·해제 반응 80ms 이하를 자동 또는 계측 플레이테스트로 확인한다.
- 10초 관찰만으로 원경·중경·바닥의 서로 다른 이동과 사무실 → 복도 → 로비 진행 방향을 식별할 수 있다.
- 모든 위험이 기준 예고 시간과 준비 → 실행 → 회복 리듬을 지키며, 150ms 인위적 반응 지연에서도 각 위험을 회피할 수 있다.
- dev collider와 실제 충돌 이벤트가 100회 결정적 리플레이에서 일치하고, 가시 실루엣 밖의 피격이 0회다.
- 1280×720, 390×844, 640×360에서 핵심 글자 크기·HUD 면적·48px 터치 영역 기준을 지킨다.

## 7. 결정 로그

- 2026-07-31: 자동 달리기, 점프+슬라이드, 3구간, 비차단 실패, 피격 기반 등급을 확정했다.
- 2026-07-31: 새 core 모듈과 DAY 4 전용 dev 화면 도입을 승인했다.
- 2026-07-31: 컨텍스트 압축 대응은 별도 대화 요약 대신 이 문서와 프로젝트 스킬을 기준으로 한다.
- 2026-07-31: 구도 비교안 A의 상단 경로·좌측 위험 거리·하단 두 입력을 채택했다.
- 2026-07-31: 도윤 달리기 시드 `minigame_character.doyun.run.right` v001을 review로 등록했다. 사용자 승인 전에는 본편에서 사용하지 않는다.
- 2026-07-31: 첫 두 장애물을 점프형 의자 → 슬라이드형 서랍으로 고정하고, 사무실 0.92배 → 복도 1.0배 → 로비 1.08배의 구간별 속도 리듬을 적용했다.
- 2026-07-31: 첫 학습 장애물 소품 `prop.office.chair`, `prop.office.drawer` v001을 review로 등록했다. 사용자 승인 전에는 본편에서 사용하지 않는다.
- 2026-07-31: OpenAI `game-studio` v0.1.2의 sprite-pipeline 보조 스크립트 3개와 참조 문서를 upstream commit `11c74d6ba24d3a6d48f54a194cd00ef3beea18f9`에서 `.agents/`로 벤더링했다.
- 2026-07-31: 장애물 소품 `prop.office.cable`, `prop.office.papers`, `prop.office.cart` v001을 동일 스타일의 review 후보로 등록했다. 사용자 승인 전에는 본편에서 사용하지 않는다.
- 2026-07-31: 코스에 이미 존재하던 로비 표지 유형을 안정 ID `prop.office.sign`으로 manifest에 추가했다.
- 2026-07-31: 로비 표지와 수집물 `prop.office.sign`, `prop.office.access_card`, `prop.office.phone`, `prop.office.backup_usb` v001을 review 후보로 등록했다. 사용자 승인 전에는 본편에서 사용하지 않는다.
- 2026-07-31: dev 반복 실행 화면에 `인트로부터` 옵션을 추가해 설명 화면과 시작 버튼을 본편 진행 없이 검수할 수 있게 했다.
- 2026-07-31: 독립 감사에서 발견한 일시정지 중 입력, 재실행 잔여 타이머, 모달 포커스 이탈, 창 전환 시 눌림 유지 문제를 수정하고 관련 통합 계약을 테스트로 고정했다.
- 2026-07-31: 위험 판정은 가로·세로 각각 9% 안쪽으로 줄여 보이는 기준 상자보다 총 18% 작게 만들고, 착지 직전 점프 버퍼의 실제 재점프 동작을 테스트했다.
- 2026-07-31: 도윤과 오피스 소품의 큰 투명 여백을 다시 그리지 않고 균일 확대·하단 중앙 정렬한 v002 review 후보로 보정했다. 사용자 승인 전에는 `active_version`으로 지정하지 않는다.
- 2026-07-31: 작은 화면에서도 “하린 항상 동행” 규칙을 지키도록 하린을 숨기지 않고, 부장→하린→도윤 추격 순서가 보이는 축소 배치를 적용했다.
- 2026-07-31: 하린 자동 보조는 콤보에 포함하지 않고, `maxCombo`는 선택 수집물의 연속 획득만 반영하도록 의미를 정리했다.
- 2026-07-31: v002 PNG의 실제 알파 경계를 `PROP_ART_FRAMING` 단일 데이터로 공유해 화면에 보이는 위험물 크기와 논리 상자를 일치시키고, 실제 충돌 상자는 양축 18% 작게 유지했다.
- 2026-07-31: 보이는 크기에 맞춘 카트가 불가능 장애물이 되지 않도록 카트 폭을 110px로 조정하고 점프 초속 760·키 해제 감쇠 0.9를 적용했다. 공통 입력 타이밍으로 64초 perfect와 수집 3개를 검증했으며, 높은 장애물은 점프를 길게 누르도록 화면에서 안내한다.
- 2026-07-31: 150ms 점프 버퍼의 성공·만료 경계, 발판 이탈 뒤 120ms 코요테 타임, 모든 위험 유형의 150ms 반응 지연, caught 완주의 단일 finish 이벤트를 행동 테스트로 고정했다.
- 2026-07-31: 공용 일시정지 메뉴와 DAY 4 저장 모달에 포커스 순환을 적용하고, 일시정지→불러오기 전환 중 미니게임이 한 프레임도 진행되지 않도록 모달이 pause 소유권을 넘겨받게 했다.
- 2026-07-31: 640×360 짧은 가로 화면에서는 HUD·코스·조작부를 고정 행으로 재배치하고, 미니게임 동안 문서 스크롤을 잠가 두 조작 버튼과 세 캐릭터가 화면 안에 남도록 했다.
- 2026-07-31: 사용자가 도윤과 오피스 소품 v002 10종을 모두 승인했다. 원본 픽셀을 변경하지 않고 `approved/`로 이동해 SHA-256·승인자·승인 시각을 기록하고 각 안정 ID의 `active_version`을 v002로 승격했다.
- 2026-07-31: 정적 런타임 resolver를 승인 manifest와 동기화하고, dev 검토 로더는 활성 버전보다 새로운 review만 덮어쓰도록 제한했다. 1280×720·390×844·640×360·880×360에서 승인본 로드와 화면 경계를 재검증했다.
- 2026-07-31: 사용자 플레이 피드백과 Super Mario Run·Celeste·Canabalt·Danganronpa V3 Death Road of Despair 조사를 반영해 화면 X 고정, 3층 패럴랙스, 입력 반응, 장애물 예고 리듬, 판정 가시화, HUD 크기, 게임 전용 액션 스프라이트를 수치형 품질 게이트로 승격했다.
- 2026-08-01: 사용자가 도윤 jump/slide, 하린 run/assist, 부장 chase/call v001 6종을 승인했다. 원본 픽셀과 SHA-256을 유지한 채 `approved/`로 이동하고, 승인자·승인 시각을 기록해 각 안정 ID의 `active_version`을 v001로 활성화했다.
- 2026-08-01: “반대방향 모습”은 캐릭터가 좌우를 번갈아 보는 것이 아니라 이동 방향은 우향으로 고정하고 팔·다리가 반대로 교차하는 다음 보폭으로 확정했다. 도윤 run_alt·하린 run_alt·부장 chase_alt v001을 별도 안정 ID의 review 후보로 등록했으며 사용자 시각 승인 전에는 본편 resolver에 넣지 않는다.
- 2026-08-01: 기존 단일 프레임 profile과 승인 시드를 보존하기 위해 새 스트립 profile 대신 같은 512×512 profile의 두 번째 보폭 ID를 사용한다. dev review 모드에서만 160ms 교대를 검수하고, 일시정지·전용 포즈·모션 감소·로드 실패 시 교대를 멈춘다.
- 2026-08-01: UI는 명령 카드를 게임 브리핑 형태로 압축하고, 부장 위험도를 3칸 압박 미터로 표시하며, 현재 구간명·장애물 부착형 행동 표식·점프/슬라이드 색상 스트립을 강화했다. 390px 모바일에서는 현재 구간을 숨기지 않고 조작부를 셸 안에 고정한다.

## 8. 개발 환경 메모

- 프로젝트 로컬 스킬은 `.agents/skills/`에 둔다. 이 폴더가 보호된 환경에서는 공식 설치·초기화 명령에 별도 쓰기 승인이 필요할 수 있다.
- Windows 기본 로캘에서 스킬 검증기가 UTF-8 한글·기호를 읽지 못하면 `python -X utf8 .../quick_validate.py <skill>`로 실행한다.
- `skill-installer`로 빠졌던 sprite-pipeline의 상위 `scripts/`와 `references/`는 `.agents/UPSTREAM.md`에 기록한 공식 버전으로 프로젝트에 고정했다. 세 보조 스크립트의 `--help` 실행까지 검증했다.
- 크로마 제거 중 `C:\tmp` 쓰기가 거부되면 저장소 내부 `.impeccable/work/day4-gait/`을 중간 캔버스·strip 작업 경로로 사용한다. 정규화가 끝난 최종 후보만 review 경로로 복사하고 승인 파일은 절대 덮어쓰지 않는다.
- DAY 4 dev 화면의 review manifest를 보려면 저장소 루트에서 HTTP 서버를 열고 `/NAN_GAME_TITLE/dev/day4-office-escape-minigame.html`로 접속한다. `NAN_GAME_TITLE`만 서버 루트로 열면 상위 `assets/art/`가 404가 되어 review 아트가 비활성화된다.
- dev 플레이 시간 입력의 최소값은 15초다. 브라우저 검수에서 15 미만을 입력하면 HTML 폼 검증 때문에 `다시 실행` submit이 발생하지 않는다.

## 9. 현재 검증과 승인 게이트

- 자동 검증: JavaScript 230개, 아트 이미지 54개·생성 로그 54개 통과.
- 정적 검증: core·런타임·DAY 4·공용 pause 스크립트 문법 검사와 `git diff --check` 통과.
- 브라우저 검증: 데스크톱과 390×844 모바일에서 시작, 키보드 점프, 일시정지 입력 차단, 재실행 초기화, 세 구간, `caught` 결과와 콜백 복귀를 확인했다. 추가로 본편 일시정지→불러오기 전환, 양방향 포커스 순환, 빈 슬롯 모달의 닫기 버튼 초기 포커스도 확인했다.
- 모바일 검증: 390×844 dev 화면에서 shell 하단 834px·footer 하단 834px·두 조작부 하단 830px, 640×360 dev 가로 화면에서 shell·footer 하단 360px·두 조작부 하단 350px로 잘림이 없다. 행동명 20px·위험 수치 18px, 현재 구간 노출, 세 캐릭터 동시 노출을 확인했다.
- 경계 폭 검증: 880×360 dev 화면에서 플레이 영역을 58px/190px/64px 행으로 유지하고 코스 높이 190px·전체 하단 360px로 851~900px 짧은 화면의 0 높이 회귀가 없음을 확인했다.
- 승인 상태: `minigame_character.doyun.run.right`와 `prop.office.*` v002 10종, 도윤 jump/slide·하린 run/assist·부장 chase/call v001 6종은 모두 approved/active이며 본편 resolver가 동일 파일을 사용한다.
- 보행 승인 게이트: `minigame_character.doyun.run_alt.right`, `minigame_character.harin.run_alt.right`, `minigame_character.boss.chase_alt.right` v001은 review·`active_version: null`이다. dev review 모드에서는 세 캐릭터가 기본/보조 프레임을 교대하고, 본편은 사용자 승인 전까지 기존 승인 기본 프레임만 사용한다.
- 최종 시각 감사: 데스크톱·390px 모바일·장애물 예고·판정 오버레이 증거에서 첫 물리 문턱, 좌측 1/3 추격 위계, 우측 위험 필드, 배경 깊이, 리뷰 게이트가 모두 통과했다.
- 보행 런타임 검증: 도윤·하린·부장님 세 캐릭터에서 기본/보조 보폭 교대, 도윤 점프 전용 포즈, 일시정지 중 520ms 프레임 고정, 오류 로그 0건을 확인했다. UI 휴리스틱 검출기의 기존 색상·타입 스케일 advisory와 장애물 화살표의 기하학 강조 warning은 실제 게임 문맥과 브라우저 결과를 대조해 비차단으로 판정했다.

## 10. 레퍼런스 근거

- [Nintendo: Super Mario Run](https://supermariorun.com/en/index.html): 캐릭터가 계속 전진하고 탭 타이밍으로 점프하는 단일 동사 자동 러너.
- [Maddy Thorson: Celeste & Forgiveness](https://maddythorson.medium.com/celeste-forgiveness-31e4a40399f1): 코요테 타임, 점프 버퍼, 정점 반중력, 코너 보정으로 플레이어에게 유리한 입력 창을 제공.
- [Adam Saltsman: Tuning Canabalt](https://www.gamedeveloper.com/design/tuning-canabalt): 원경·중경·전경을 서로 다른 비율로 움직여 속도와 전방 가독성을 함께 만드는 자동 러너 카메라.
- [Spike Chunsoft: Danganronpa V3](https://www.spike-chunsoft.com/games/danganronpa-v3-killing-harmony/): 16인 탈출 압박과 미니게임의 원작 맥락. DAY 4에는 함정 리듬과 동행 긴장만 차용한다.
