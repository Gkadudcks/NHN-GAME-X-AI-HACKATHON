# Art source assets

This directory contains source art, prompt records, generation metadata, and the engine-neutral asset registry. Start with `docs/art/ART_PIPELINE.md`. Files under any `approved/` directory are immutable.

